@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to login page, passing the current URL so we can
        // return the user to where they were after a successful login
        Navigation.NavigateTo($"/?returnUrl={Uri.EscapeDataString(Navigation.Uri)}", replace: true);
    }
}